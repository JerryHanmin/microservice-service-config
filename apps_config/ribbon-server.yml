server:
  port: 9000
eureka:
  instance:
    virtualHostName: ribbon-server
    appGroupName: testGroup
    preferIpAddress: false  #true: display ip address false: display pc name
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/discovery/eureka/
